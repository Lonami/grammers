export enum ScreenEnum { login, main, settings }

export enum LoginStepEnum { phone, code, password }

// @rust-attr(derive(arbitrary::Arbitrary))
export enum ChatKindEnum { user, user-self, chat, channel }
